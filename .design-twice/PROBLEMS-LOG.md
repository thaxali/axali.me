# Problems log — axali.me Studio rebrand

## 2026-09-16
- Nacelle and "By Ax" font files are not accessible from this session (live in Ax's Google Drive "The Studio" folder). Typography pass is blocked on exact fonts until supplied.
- The 23 existing project entries in `index.html` are bespoke-per-entry (hand-tuned CSS per `data-product-id`), not a generic template. Needs a decision on whether to generalize before extending.
- Prior architecture doc (`axalime_architecture.md`) says aesthetic direction was "not yet locked" as of May 2026, but the live site has since shipped its own design — that note is stale and should be updated once the Studio rebrand ships.
